#!/usr/bin/env zsh
# Make any audio a ringtone (30s AAC)
if [[ -z "$1" ]]; then
  echo "Usage: tom4a <input_file>"
  exit 1
fi
out="${1%.*}.m4a"
[[ "$out" == "$1" ]] && out="${1%.*}-converted.m4a"
ffmpeg -i "$1" -t 30 -c:a aac "$out"
